# Environment Configuration
NODE_ENV=development
PORT=3000

# Database Configuration Options (choose based on your environment)

# Option 1: Docker PostgreSQL (RECOMMENDED - consistent across machines)
DATABASE_URL="postgresql://postgres:flit1234567@localhost:5433/flit_dev?schema=public"

# Option 2: Local PostgreSQL (requires PostgreSQL installation)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/flit_dev?schema=public"

# Option 3: SQLite (easiest for quick testing - no setup required)
# DATABASE_URL="file:./dev.db"

# Option 4: Production/Staging - AWS RDS (requires VPC access or public accessibility)
# DATABASE_URL="postgresql://postgres:password@flit.cetssm8wks3n.us-east-1.rds.amazonaws.com:5432/postgres?schema=public"

# JWT Configuration (for future auth integration)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Clerk Authentication (to be configured later)
# CLERK_SECRET_KEY=
# CLERK_PUBLISHABLE_KEY=
